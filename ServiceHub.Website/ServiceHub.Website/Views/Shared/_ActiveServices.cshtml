@model IEnumerable<ServiceHub.Website.Models.MyServiceItem>

<div class="table">
    <div class="row headerRow">
        <div>Bidding Completion</div>

        <div>Estimated Service Due</div>

        <div>Lowest Bid</div>

        <div>Service Reference</div>

        <div>Status</div>
        <div></div>

    </div>
    @foreach (var item in Model.Where(o => !o.IsCompleted))
    {
        <div class="row">

            <div>@Html.DisplayFor(o => item.BiddingCompletionDate)</div>

            <div>@Html.DisplayFor(o => item.ServiceDue)</div>
            
            <div>@Html.DisplayFor(o => item.LowestBid)</div>

            <div>@Html.ActionLink(item.Reference, "Service", "Client", new { serviceId = item.ServiceId }, null)</div>

            <div>@Html.DisplayFor(o => item.Status)</div>

            <div>@Html.ActionLink("Cancel Service", "CancelService", "Client", new { serviceId = item.ServiceId }, null)</div>

        </div>

    }
</div>
